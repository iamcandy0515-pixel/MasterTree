{
  "name": "flutter_admin_app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "flutter build apk --release",
    "build:web": "flutter build web --release",
    "dev": "flutter run -d web-server --web-port=5050 --web-renderer html --web-hostname localhost",
    "test": "flutter test",
    "clean": "flutter clean",
    "lint": "flutter analyze ."
  }
}
