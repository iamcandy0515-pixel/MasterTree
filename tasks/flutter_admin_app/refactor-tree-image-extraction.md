# Task: 수목 이미지 추출 상세 화면 UI 및 로직 개선

## 상태 기록 (Plan)

- **목적**: 수목 이미지 관리 화면의 텍스트 및 버튼 명칭을 명확히 하고, 드라이브 추출 이미지 위주의 작업 흐름으로 개선
- **요구사항 분석**:
    1. **텍스트 변경**:
        - 화면 제목: '수목일람 상세' -> '수목 이미지 추출 상세'
        - 서브 타이틀: '가문비나무 이미지관리' -> '가문비나무'
        - '구글 드라이브 동기화' -> '드라이브 이미지 추출'
        - '저장하기' -> 'DB저장'
    2. **이미지 로직 변경**:
        - 초기 진입 시: DB에 저장된 '저장 이미지' 표시
        - 추출 시: 'DB저장 이미지'는 삭제(화면에서 제거)하고 '드라이브 추출 이미지'만 표시/등록
    3. **UI 개선**:
        - 이미지 Box 사이즈 최적화 (이미지 사이즈에 맞게 축소)
- **대상 파일**: `lib/features/trees/screens/tree_detail_screen.dart`

## 실행 (Execute)

1. **화면 텍스트 및 UI 레이아웃 수정**
    - [x] AppBar title 변경 및 서브 타이틀 형식 수정 (수목 이름 동적 표시)
    - [x] 버튼 텍스트 변경 (드라이브 이미지 추출, DB저장)
    - [x] 이미지 Grid의 AspectRatio 및 여백 조정을 통한 Box 사이즈 최적화
2. **이미지 관리 상태 로직 수정**
    - [x] 초기화 시 `widget.tree.images`를 `_stagedImages`에 로드
    - [x] `_fetchFromDrive` 호출 시 기존 `_stagedImages`를 초기화하고 추출된 이미지만 할당하도록 로직 변경
    - [x] 'DB저장' 시 추출된 이미지 리스트만 최종적으로 전달되도록 검증 로직 추가
3. [x] 최종 테스트 및 Linter 체크 완료

## 사후 점검 (Review)

- **완료된 결과 (Result)**:
    - 화면 제목 및 버튼 명칭 요구사항 반영 완료.
    - 드라이브 추출 시 기존 DB 이미지를 클리어하고 추출된 결과만 유지하는 로직 구현.
    - 이미지 박스 크기 최적화 (childAspectRatio 1.1).
- **향후 문제점 및 리스크 분석 (Risk Analysis)**:
    - 기존 저장된 이미지가 추출 시 화면에서 사라지므로, 사용자가 실수로 추출했을 때의 복구 방안(취소 기능) 고려 필요
    - 이미지 타입별(main, bark 등) 1:1 매칭 확인 루틴 강화 필요
