# Task: Quiz Screen UI & Image Filter Update

## 1. 상태 기록 (Plan)

- **목적**: '수목 식별 학습' 화면의 UI 레이아웃을 조정하고 이미지 보안(블러 처리)을 강화함.
- **작업 범위**:
    1.  **버튼 위치 변경**: '그만하기' 버튼을 '힌트보기' 텍스트 옆(우측)으로 이동.
    2.  **이미지 블러 필터**:
        - 수목 이미지 상단 1/4 영역에 강력한 블러 적용.
        - 수목 이미지 하단 1/4 영역에 강력한 블러 적용.
        - 기존 하단 1/3 블러 처리를 제거하고 1/4로 조정 및 강도 상향.

---

## 2. 실행 (Execute)

- [x] `flutter_user_app/lib/screens/quiz_screen.dart` 수정 - 완료
    - `_buildMainImage` 함수 내의 `BackdropFilter` 로직 업데이트 (상단/하단 1/4 블러).
    - `_buildHintSectionWithButton` 함수 내의 버튼 배치 수정.
- [x] 레이아웃 이상 여부 확인 - 완료

---

## 3. 사후 점검 (Review)

- **완료된 결과(Result)**:
    - **버튼 재배치**: 기존 하단에 있던 '그만하기' 버튼을 '힌트 보기' 섹션의 상단 Row 우측으로 이동시켜 접근성을 개선했습니다.
    - **강화된 블러 필터**: 상단 1/4 및 하단 1/4 영역에 `sigma: 25` 수준의 강력한 블러와 블랙 반투명 오버레이를 적용하여 학습 난이도를 조절했습니다.
- **향후 문제점 및 리스크 분석(Risk Analysis)**:
    - **UI 밀집도**: 버튼이 상단으로 올라감에 따라 힌트 섹션의 가독성을 확인해야 합니다.
    - **난이도 과잉**: 블러가 너무 강력하여 단서가 부족할 경우 힌트 의존도가 높아질 수 있습니다.
