class QuizDashboardController {
  // Stats Mock data (to be replaced with /api/user-quiz/stats API)
  final int overallAccuracy = 68;
  final int totalAttempts = 245;

  final List<double> trends = [40, 45, 60, 50, 75, 80, 90];

  void init() {
    // Fetch real data from API
  }
}
